# Local Development Environment Variables
# Copy this file to .env.local and fill in your values
# Run: cp .env.local.example .env.local

# Flask Configuration
SECRET_KEY=your-local-secret-key-change-this
DEBUG=True
PORT=5000

# Database Configuration (for local Docker postgres)
DATABASE_URL=postgresql://dcabot:dcabot_dev_password@localhost:5435/dcabot_dev

# Security (generate with: python -c "import base64, os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())")
ENCRYPTION_KEY=your-encryption-key-here

# Optional: Telegram Notifications (for testing bots)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# TELEGRAM_CHAT_ID=your_telegram_chat_id
