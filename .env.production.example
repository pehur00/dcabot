# Production Environment Configuration (camproute-server)
# Copy to .env on production server
# Path: /var/www/dcabot-saas/.env

# ============================================================================
# PRODUCTION DATABASE - Digital Ocean Managed PostgreSQL
# ============================================================================
DATABASE_URL=postgresql://diptrader:STRONG_PASSWORD_HERE@private-dbaas-db-10215807-do-user-15659652-0.e.db.ondigitalocean.com:25060/diptrader?sslmode=require

# Flask Application (Production)
# Generate SECRET_KEY with: python -c "import os; print(os.urandom(32).hex())"
SECRET_KEY=production_secret_key_from_openssl_rand_minimum_32_chars
FLASK_ENV=production
DEBUG=False

# Security - API Key Encryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: Use a different key than development!
ENCRYPTION_KEY=production_fernet_encryption_key_here

# Bot Mode
BOT_ID=
