# Environment Variables for Render Deployment
# Set these in Render Dashboard when deploying

# ============================================================================
# DATABASE (Digital Ocean Managed PostgreSQL)
# ============================================================================
# Public connection URL (for Render to connect):
DATABASE_URL=postgresql://diptrader:YOUR_PASSWORD@private-dbaas-db-10215807-do-user-15659652-0.e.db.ondigitalocean.com:25060/diptrader?sslmode=require

# Note: Replace YOUR_PASSWORD with actual password
# Database must allow connections from these Render IPs:
# - 3.75.158.163
# - 3.125.183.140
# - 35.157.117.28
# - 74.220.51.0/24
# - 74.220.59.0/24

# ============================================================================
# SECURITY KEYS
# ============================================================================

# Flask secret key (auto-generated by Render, or generate manually):
# SECRET_KEY=<auto-generated>

# Encryption key for API credentials (generate once, keep forever):
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# IMPORTANT: Keep this key safe! Lost key = can't decrypt user API keys
# Back up this key somewhere secure (password manager, encrypted file, etc.)

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
FLASK_ENV=production
DEBUG=false
PYTHON_VERSION=3.11.0

# ============================================================================
# NOTES
# ============================================================================
# These env vars are set in Render Dashboard:
# 1. Render Dashboard → Service → Environment
# 2. Add each variable
# 3. Mark sensitive ones as "Secret" (not visible in logs)
#
# For dcabot-saas-web: Set DATABASE_URL, ENCRYPTION_KEY (SECRET_KEY auto-gen)
# For dcabot-saas-scheduler: Set DATABASE_URL, ENCRYPTION_KEY
